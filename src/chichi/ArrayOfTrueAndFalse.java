package chichi;

public class ArrayOfTrueAndFalse {
    public boolean[] evenAndOdd(int[] sample) {
        boolean[] number = new boolean[sample.length];
        for (int count = 0; count < sample.length; count++) {
            sample[count] = sample[count];

        }
        return number;
    }
}
